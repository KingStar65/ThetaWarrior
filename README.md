This project is built with ReactJS, TailwindCSS, RESTAPI and POSTgresql.

ThetaWarrior is a web based(no mobile support yet) Options Tracker.

It's main purpose is to calculate the collateral values of trades so that I could know the available cash i can use safely. There are functionalities to add individual information of options trades and it will be displayed on the dashboard.